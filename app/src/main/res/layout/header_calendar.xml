<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="16dp">

    <TextView
        android:id="@+id/tv_status"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Your upcoming certification reminders will appear here."
        android:textAllCaps="true"
        android:fontFamily="sans-serif-medium"
        android:textSize="17sp"
        android:textStyle="normal"
        android:layout_marginBottom="16dp" />

    <com.prolificinteractive.materialcalendarview.MaterialCalendarView
        android:id="@+id/materialCalendarView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:mcv_arrowColor="@android:color/white"
        app:mcv_weekDayLabels="@array/custom_weekday_labels"
        app:mcv_dateTextAppearance="@style/CalendarDayGreyText" />

</LinearLayout>
