<!-- res/layout/fragment_reminders.xml -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/layout_reminders"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp">

    <!-- TextView na zobrazenie statusu alebo inštrukcií -->
    <TextView
        android:id="@+id/tv_status"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Your upcoming certification reminders will appear here."
        android:textSize="18sp"
        android:layout_marginBottom="20dp" />

    <!-- ListView pre zobrazenie certifikátov, ktoré je potrebné obnoviť -->
    <ListView
        android:id="@+id/list_view_expiring"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:divider="@android:color/darker_gray"
        android:dividerHeight="1dp" />

</LinearLayout>
